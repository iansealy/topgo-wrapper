To run the topGO example (in the top level directory, not the example
directory):

perl -Ilib script/run_topgo.pl \
--dir tmp --input_file example/example.tsv \
--gene_field 10 --p_value_field 8 \
--fold_change_field 64 \
--name_field 14 --description_field 15 \
--go_terms_file data/danio_rerio_e76_go.txt \
--header
